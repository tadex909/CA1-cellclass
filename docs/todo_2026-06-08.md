# Todo - 08/06/2026

1. ✅ Instead of ruling out all interneurons altogether, calculate their SSI and test whether each interneuron's SSI is significant relative to that neuron's circular-shift SSI null distribution.

2. ✅ Generate a CSV with the following columns:
   `session_id`, `age_group`, `n_sure_pyr`, `n_uns_pyr`, `n_interneuron_with_significant_SSI`, `sum_of_the_previous_3_columns`.

3. [ ] Modify the Bayesian decoder so it has an option to train on all laps, including the laps being tested.

4. [ ] Generate a script or notebook that creates a confusion matrix of real position vs decoded position for the trials with the highest and lowest decoding error.

5. [ ] Check sessions that have `PO`, `PO2`, and `PO3` trials, to assess stability or remapping across those conditions.
